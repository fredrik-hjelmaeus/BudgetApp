import { render, screen } from '../test-utils/context-wrapper';
import App from '../App';

describe('navigation through all month pages', () => {
  test('initial state correct in month', () => {});
  test('guide is activated correct from user profile modal from month', () => {});
  test('navigate to other month works and updates all state', () => {});
  test('click to year and back to month works', () => {});
  test('navigate to savings summary works', () => {});
  test('add to budget expands and contracts correct', () => {});
  test('open user details modals works', () => {});
  //render(<App />)
});
