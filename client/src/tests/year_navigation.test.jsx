import { render, screen } from '../test-utils/context-wrapper';
import App from '../App';

describe('navigation through all year pages', () => {
  test('initial state correct in year', () => {});
  test('guide is activated on new user', () => {});
  test('initial year state correct after exiting guide for new user', () => {});
  test('initial year state correct after exiting guide for user with own presets created', () => {});
  test('navigate to expense summary works', () => {});
  test('navigate to income summary works', () => {});
  test('navigate to savings summary works', () => {});
  test('navigate back to balance summary works', () => {});
  test('open user details modals works', () => {});
  //render(<App />)
});
